{
  "buildCommand": "cd vinya-web && npm install && npm run build",
  "outputDirectory": "vinya-web/dist",
  "installCommand": "echo skip",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
